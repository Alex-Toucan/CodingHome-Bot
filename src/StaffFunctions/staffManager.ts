// staffManager.ts / handles all the available functions of the server team.
// Concept: index.ts -> invokes function of staffManager.ts ->
// invokes the function of the specified file -> function returns the result to staffManager.ts ->
// staffManager.ts returns result to index.ts -> index.ts handles the result

// only specific roles will have access to these functions.

// staffManager.ts and functions'll be in the "StaffFunctions" directory.
